import { StoresFilterPipe } from './stores-filter.pipe';

describe('StoresFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StoresFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
